package bpi.most.server.services.gwtrpc;

import java.util.Observable;
import java.util.Observer;

/**
 * The DpChangedEventListener listen to changed events on a Datapoint after a new measurement is assigned
 *
 * @author Johannes Weber
 */
public class DpChangedEventListener implements Observer {
	/**
	 * @see import {@link java.util.Observer}
	 */
	public void update(Observable o, Object arg) {}
}
